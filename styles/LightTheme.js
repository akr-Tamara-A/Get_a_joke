import {colors1, colors2, colors3, colors4} from './themes';

const colorTheme = colors4;

const LightTheme = {
  dark: false,
  colors: {
    primary: colorTheme[3],
    background: colorTheme[2],
    background2: colorTheme[3],
    card: colorTheme[2],
    text: colorTheme[5],
    border: colorTheme[5],
    notification: colorTheme[1],
  },
};

export default LightTheme;
