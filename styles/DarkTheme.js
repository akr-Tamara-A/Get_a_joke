import {colors1, colors2, colors3, colors4} from './themes';

const colorTheme = colors4;

const DarkTheme = {
  dark: true,
  colors: {
    primary: colorTheme[4],
    background: colorTheme[5],
    background2: colorTheme[4],
    card: colorTheme[5],
    text: colorTheme[3],
    border: colorTheme[3],
    notification: colorTheme[5],
  },
};

export default DarkTheme;
